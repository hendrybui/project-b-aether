# Melody Suite — Standalone Flask Music Analysis & Composition
# Python 3.12 compatible. No TensorFlow/torch/magenta needed.

flask>=3.0
librosa>=0.10
soundfile>=0.12
numpy>=1.26
scipy>=1.11
pretty_midi>=0.2
music21>=9.1
pytest>=8.0          # dev/test only — run with: venv/bin/python -m pytest tests/
