{
  "name": "melody-sheet-js-tests",
  "version": "1.0.0",
  "private": true,
  "description": "jsdom unit tests for static/score_preview.js, run via `node --test` (and wired into pytest)",
  "scripts": {
    "test": "node --test score_preview.test.js"
  },
  "devDependencies": {
    "jsdom": "^24.1.0"
  }
}
